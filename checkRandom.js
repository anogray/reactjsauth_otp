            
            
            
            // let OTP = Math.ceil(Math.random()*1000000);

            while(1){
                let OTP = Math.ceil(Math.random() * (999999 - 100000 + 1) + 100000);

                console.log(OTP)
            }
